#ifndef JSONCONFIG_H__
#define JSONCONFIG_H__

// seams no effect to overrive RAPIDJSON_ASSERT to throw instead of abort
// see rapidjson.h:402
// #define RAPIDJSON_ASSERT(x) do { if (!(x)) throw std::exception(); } while(0)

#include "rapidjson/document.h"

#endif /* end of include guard: JSONCONFIG_H__ */
